No blocking ambiguities remain after Phase 1.

Implementation will proceed with these resolved assumptions:
- PR `#40` review comments define the required scope.
- The existing PR branch is the correct place to implement the fix.
- The benchmark should keep its current reported outputs while moving the metric calculations into reusable helpers under `utils/stability.py`.
