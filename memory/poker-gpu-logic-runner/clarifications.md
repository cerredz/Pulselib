No blocking ambiguities remained after Phase 1.

Assumptions carried into implementation:
- The deliverable is a standalone executable script under `scripts/Poker/`.
- The script should run all cases from a single `main()` entrypoint and print multi-line descriptions before each case.
- Coverage will prioritize real `PokerGPU` logic seams and known regressions already evidenced in `tests/poker/` and `memory/tickets/`.
